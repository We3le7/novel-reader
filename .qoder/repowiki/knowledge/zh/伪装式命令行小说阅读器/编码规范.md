- 采用“伪装模式”设计，阅读界面以代码注释风格（`// ` 前缀）展示文本，底部进度条显示为 `Compiling assets`，Esc 键可切换至模拟 CI/CD 日志的恐慌界面。
- 实现智能章节导航，通过正则表达式识别中英文章节标题建立索引，支持 `n/p` 键在章节间跳转，并在上一章逻辑中增加了行偏移容错处理。
- 采用双路径状态持久化策略，优先尝试写入用户配置目录，若权限不足则自动回退至项目根目录下的隐藏 JSON 文件，确保状态保存的鲁棒性。